location /billing/admin/ {
    access_by_lua_file /etc/openresty/lua/admin_access.lua;
    proxy_set_header Authorization "";
    proxy_set_header Cookie "";

    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $remote_addr;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header X-Forwarded-Port $server_port;

    resolver 127.0.0.11 valid=5s;
    set $backend_billing_admin http://blockchain-services:8080;
    proxy_pass $backend_billing_admin;
}

location /billing/ {
    access_by_lua_file /etc/openresty/lua/billing_access.lua;
    # Preserve the short-lived Marketplace service JWT for read-only
    # institutional billing calls. billing_access.lua only bypasses the
    # edge admin guard for the explicitly read-only paths, and the backend
    # validates the JWT and billing:read scope before serving them.
    proxy_set_header Authorization $http_authorization;
    proxy_set_header Cookie "";
    # Enable CORS for API endpoints
    if ($cors_allow_origin = "DENY") {
        return 403;
    }
    if ($cors_allow_origin != "") {
        add_header 'Access-Control-Allow-Origin' $cors_allow_origin always;
        add_header 'Access-Control-Allow-Credentials' 'true' always;
        add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS, PUT, DELETE' always;
        add_header 'Access-Control-Allow-Headers' 'Authorization, Content-Type, X-Requested-With' always;
        add_header 'Access-Control-Max-Age' 1728000 always;
        add_header 'Vary' 'Origin' always;
    }

    # Handle preflight requests
    if ($request_method = 'OPTIONS') {
        return 204;
    }

    # Proxy configuration
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $remote_addr;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header X-Forwarded-Port $server_port;

    # Timeout configuration for blockchain operations
    proxy_read_timeout 60s;
    proxy_connect_timeout 10s;
    proxy_send_timeout 10s;

    # Resolver for service discovery
    resolver 127.0.0.11 valid=5s;

    # Proxy to blockchain-services billing API (using variable for dynamic resolution)
    set $backend_billing http://blockchain-services:8080;
    proxy_pass $backend_billing;
}
